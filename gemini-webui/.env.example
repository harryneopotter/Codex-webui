# Network
PORT=5056
HOST=127.0.0.1
# Optional bearer token for write routes (POST)
WEBUI_TOKEN=

# Gemini CLI Configuration
# The Gemini CLI command (defaults to 'gemini')
AI_CMD=gemini

# Additional arguments to pass to Gemini CLI
# Example: AI_ARGS=--include-directories ../lib,../docs
AI_ARGS=

# Default Model Settings
GEMINI_MODEL=gemini-2.5-pro      # Default model
GEMINI_TEMPERATURE=0.7           # Default temperature (WebUI only)
GEMINI_MAX_TOKENS=1024           # Default max tokens (WebUI only) 
GEMINI_TOP_P=0.9                 # Default top-p (WebUI only)

# Gemini CLI Authentication
# Option 1: API Key (get from https://aistudio.google.com/apikey)
# GEMINI_API_KEY=your_api_key_here

# Option 2: Vertex AI
# GOOGLE_API_KEY=your_vertex_ai_key
# GOOGLE_GENAI_USE_VERTEXAI=true

# Option 3: OAuth with Google Cloud Project (for Code Assist)
# GOOGLE_CLOUD_PROJECT=your_project_name

# Common Gemini CLI Options:
# AI_ARGS=--include-directories ../lib,../docs  # Include additional directories
# AI_ARGS=-m gemini-2.5-flash                   # Use specific model
# AI_ARGS=-p "Non-interactive prompt"           # Non-interactive mode
# AI_ARGS=--checkpointing                       # Enable checkpointing
